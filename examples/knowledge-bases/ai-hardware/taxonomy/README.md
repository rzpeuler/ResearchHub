# Knowledge Taxonomy Assets

Classification vocabularies for industries, themes, technologies, and value
chains belong here.

The production SW Level-1 catalog is `sw-level-1.yaml`. Its 31 stable item IDs
use the `sw:*` namespace; the AI Hardware graph is linked from
`sw:electronics`.
