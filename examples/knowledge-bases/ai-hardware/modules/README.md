# Knowledge Module Assets

Industry-specific comparison, roadmap, market, competition, capacity, and
supply-chain modules belong here.

`company` is an Entity type and a valid module `targetEntity`, but it is not a
Module type. Module-to-Entity availability is declared by the runtime Module
Registry rather than inferred from every module file.
