# Knowledge Access Skill

The Knowledge Access Skill provides deterministic, read-only access to
repository-level Knowledge assets through `index.ts`.

It does not call an LLM, update Knowledge, make investment decisions, or access
external systems. Workflow owns business orchestration and lifecycle updates.
