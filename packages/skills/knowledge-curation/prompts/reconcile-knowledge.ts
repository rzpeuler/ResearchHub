export const RECONCILE_KNOWLEDGE_PROMPT = `Reconcile the supplied candidate groups against the supplied precise existing Knowledge context. Return exactly one decision for every supplied candidate. Copy each candidateId exactly; do not invent candidate IDs. existingRefs must be a subset of that candidate's supplied existingRefs; do not use context-only Knowledge IDs as decision refs, invent Knowledge refs, or return refs when none are supplied. Apply the supplied Claim and Relation semantics: facts seek consistency, supported forecast/viewpoint divergence may coexist, and material semantic Claim changes require supersession. Return semantic decisions only; do not return Writer operations, durable identifiers, revisions, or storage paths.`
