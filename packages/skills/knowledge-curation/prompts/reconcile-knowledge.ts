export const RECONCILE_KNOWLEDGE_PROMPT = `Reconcile the supplied candidate groups against the supplied precise existing Knowledge context. Return exactly one semantic decision for every supplied candidate. Apply the supplied Claim and Relation semantics: facts seek consistency, supported forecast/viewpoint divergence may coexist, and material semantic Claim changes require supersession. Return semantic decisions only; do not return Writer operations, durable identifiers, revisions, or storage paths.`
