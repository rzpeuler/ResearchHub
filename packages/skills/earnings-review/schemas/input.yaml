type: object
required: [symbol, companyName, period, asOf]
properties:
  symbol: { type: string, pattern: '^[0-9]{6}$' }
  companyName: { type: string, minLength: 1 }
  period: { type: string, minLength: 1 }
  asOf: { type: string, format: date-time }
