type: object
required: [skillId, subject, asOf, template, sections, evidence, variances, guidance, thesisImpact]
properties:
  skillId: { const: earnings-review }
  subject: { type: string }
  asOf: { type: string, format: date-time }
  template: { const: earnings-review-report }
  sections: { type: array }
  evidence: { type: array }
  variances: { type: array }
  guidance: { type: string, enum: [raised, maintained, lowered, not-provided] }
  thesisImpact: { type: string, enum: [positive, negative, neutral, undetermined] }
