# Knowledge Validation Fixtures

This directory contains the deterministic AI Hardware fixture dataset used by
Knowledge Loader, Access Skill, Validation Skill, and integration tests.

- `valid/` contains loadable assets.
- `invalid/` contains deliberately broken assets used by validation tests.
