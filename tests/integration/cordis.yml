# Documentation fixture for the real Cordis composition used by the integration test.
# The test mounts the same plugins programmatically so it can inject a deterministic
# mock adapter and assert the live handles directly.
- id: researchhub-integration-validation
  name: './extension.ts'
  config:
    skillRoot: './packages/skills'
- id: researchhub-market-capability-validation
  name: './market-capability-extension.ts'
- id: researchhub-event-analysis-validation
  name: './event-analysis-extension.ts'
  config:
    skillRoot: './packages/skills'
